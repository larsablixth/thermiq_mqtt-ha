name: Push actions

on:
  push:
    branches:
      - master
      - dev

jobs:
  validate:
    runs-on: "ubuntu-latest"
    name: Validate
    steps:
        - uses: "actions/checkout@v4"
        - name: HACS validation
          uses: "hacs/action@main"
          with:
            category: "integration"
            ignore: brands

        - name: Hassfest validation
          uses: "home-assistant/actions/hassfest@master"

  style:
    runs-on: "ubuntu-latest"
    name: Check style formatting
    steps:
        - uses: "actions/checkout@v4"
        - uses: "actions/setup-python@v5"
          with:
            python-version: "3.x"
        # Single source of truth for the black pin: requirements_test.txt
        - run: python3 -m pip install "$(grep '^black==' requirements_test.txt)"
        - run: black --check .

  test:
    runs-on: "ubuntu-latest"
    name: Run tests
    steps:
        - uses: "actions/checkout@v4"
        - uses: "actions/setup-python@v5"
          with:
            python-version: "3.14"
        - run: python3 -m pip install -r requirements_test.txt
        - run: python3 -m pytest tests -v
        - name: Type check
          run: python3 -m mypy

